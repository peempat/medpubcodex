# Web App dependencies (research demo).
#
# The benchmark ran on transformers 5.14.1 / bitsandbytes 0.49.2 with a CUDA
# build of PyTorch 2.11. Pin close to that when running the model tabs.

gradio>=5.0,<6
pandas>=2.0
transformers==5.14.1
accelerate>=1.14
huggingface_hub>=1.2,<2
sentencepiece>=0.2,<0.3
psutil>=5.9
python-dotenv>=1.0

# Install a CUDA build of torch for your platform, for example:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.5

# Required only for the INT8 / INT4-NF4 checkpoints. Needs a CUDA GPU.
bitsandbytes==0.49.2 ; platform_system != "Darwin"

# Tests
pytest>=8.0
